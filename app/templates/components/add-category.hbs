<div class="add">
  {{#if addNewCategory}}
  <h1>New Category</h1>
    <form >
      <div class="form-group">
        <label for="category">Category</label>
        {{input value=category id="category"}}


    <button {{action 'saveCategory1'}}>Save Category</button>
    </div>
    </form>

{{else}}
  <button class = "addcategorybtn" {{action 'categoryFormShow'}}>New Category</button>
{{/if}}
</div>
